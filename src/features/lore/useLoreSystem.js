// useLoreSystem.js (feature-level)
// Feature-scoped wrapper around the top-level useLoreSystem hook.
// Adds feature-specific logic for the LoreInspector and LoreMemoryPanel.
// Consumes LoreContext.
